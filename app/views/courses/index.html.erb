<p style="color: green"><%= notice %></p>

<% content_for :title, "Courses" %>

<h1>Courses</h1>

<div id="courses">
  <% @courses.each do |course| %>
    <%= render course %>
    <p>
      <%= link_to "Buy this course", new_payment_path(course_id: course.id) %>
    </p>
  <% end %>
</div>

<%= link_to "New course", new_course_path %>
